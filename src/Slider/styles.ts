import { StyleSheet } from 'react-jss'

const sliderStyles: StyleSheet<any> = {
  rtgList: {
    display: 'flex',
    position: 'relative',
    margin: 'auto'
  },
  rtgWrapper: {
    position: 'absolute',
    left: 0,
    right: 0
  }
}

export default sliderStyles
